{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}Nos produits- Move Your Body {% endblock %}

{% block content %}

<div class="row">
    <div class="col-md-5 mt-5">
        <img src="/images/products/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid">
        </div>
    {# <h1 class="d-flex justify-content-center my-6">{{ product.name }}</h1> #}

    <div class="col-md-7 my-auto">
        {# {% for product in products %} #}
        <h3>{{ product.name }}</h3>
        <p>{{ product.subtitle }}</p>
        <span>{{ (product.price / 100)|number_format(2, ',','.') }} €</span>
        <hr>
        <p>
            {{ product.description }}
        </p>
        <a href="{{ path('add_to_cart', { 'id' : product.id }) }}" class="btn btn-primary">Ajouter au panier</a>
        </div>
        </div>

        <hr>
         <h3>Nos meilleures ventes</h3>
        <p>Decouvrez les articles les plus vendus. Une sélection des meilleurs équipements running des plus grandes marques vous est proposée : chaussures running, vêtements running, montres cardio GPS, accessoires trail et marche nordique. Sur le site Boutique Marathon, vos achats sont traités et expédiés dans les 24h pour une livraison rapide et offerte dès 90€...</p>
       <div class="row">
       {% for product in products %}
            <div class="col-md-3">
                {% include 'product/single_product.html.twig' %}
            </div>
       {% endfor %}
       </div>
            {# <div class="col-md-4"> #}

    {# {{ dump(product) }} #}

{% endblock %}
